// Force dark mode for all screens in this app.
// This makes `useThemeColor` choose values from Colors.dark,
// and also makes navigation theming consistently dark.

export function useColorScheme() {
  return 'dark';
}

